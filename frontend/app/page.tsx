export default function HomePage() {
  return (
    <main style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      <h1>Mood Music Recommendation</h1>
      <p>Your backend is running on Render.</p>
      <p>Connect this frontend to the Render API URL using NEXT_PUBLIC_API_URL.</p>
    </main>
  );
}
